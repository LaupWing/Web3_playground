const networkConfig = {
   31377: {
      name: "localhost",
      ethUsdPriceFeed: "",
      callbackGasLimit: "",
   },
   5: {
      name: "localhost",
      ethUsdPriceFeed: "",
      callbackGasLimit: "",
   },
}

const developmentChains = ["hardhat", "localhost"]

module.exports = {
   networkConfig,
   developmentChains,
}
